import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;


public class string {
    
    public static void main(String[]argumentos) throws IOException{
        
        
       BufferedReader teclado = new BufferedReader (new InputStreamReader(System.in));
        for(;;){
       String numero = teclado.readLine();
       String error = numero.substring (0,1);
       int err= Integer.parseInt(error);
       if (err==0)
    	   System.exit(0);
        int par = 0;
        int imp = 0;
        int fin = 0;
        
        
        for (int i = 0; i<=numero.length()-1;i=i+2){
            
           
            char a = numero.charAt(i);
            	
            par = par + Integer.parseInt(a+"");
        }
        
        int cad = numero.length();
       
        
        String combinacion = numero.substring (1,cad);
        for (int r = 0; r<=combinacion.length()-1;r=r+2){
      
            	
                char w = combinacion.charAt(r);
                	
                imp = imp + Integer.parseInt(w+"");
            }
        fin = par - imp;

            if (fin==0 || fin%11==0)
            
        System.out.println(numero+ " Is a multiple of 11.");
            else 
            	System.out.println(numero+" Is not a multiple of 11.");
        
       
        

    }

       
    }
}